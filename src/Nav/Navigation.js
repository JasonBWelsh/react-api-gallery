import React from 'react';
import {NavLink} from 'react-router-dom';

//creates the links used for the images.
const Navigation = props => {
  return(
    <div>
        <nav className="main-nav">

        </nav>
    </div>
  )
}

export default Navigation;
