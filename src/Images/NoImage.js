import React from 'react';

// renders display for unfound results
// rendered in ImageList
const NoImage = props => (
  <div className="not-found">
    <h3>No search results</h3>
  </div>
);

export default NoImage;
